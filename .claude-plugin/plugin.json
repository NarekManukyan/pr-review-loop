{
  "name": "pr-review-loop",
  "description": "PR review toolkit for Claude Code: /review-pr panel (inline) + /review-pr-watch loop, /review-pr-slack (HTML report + Slack verdict) + /review-pr-slack-watch loop with reactions as PR state. Self-improving per-repo review memory learns from developer responses across rounds; never overrides CLAUDE.md/ADRs.",
  "version": "1.8.0",
  "author": {
    "name": "NarekManukyan"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/ensure-and-nudge.sh\"",
            "timeout": 20,
            "statusMessage": "Preparing review memory..."
          }
        ]
      }
    ]
  }
}
